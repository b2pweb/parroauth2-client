name: Build

on:
  push:
    branches:
      - master
      - '[1-9].[0-9]+'
  pull_request:
    branches:
      - master
      - '[1-9].[0-9]+'

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        php-versions: ['8.1', '8.2', '8.3', '8.4', '8.5']
    name: PHP ${{ matrix.php-versions }}

    steps:
    - uses: actions/checkout@v2
    
    - name: Set Timezone
      uses: szenius/set-timezone@v1.0
      with:
        timezoneLinux: "Europe/Paris"
    
    - name: Install PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: ${{ matrix.php-versions }}
        extensions: json
        ini-values: date.timezone=Europe/Paris
    - name: Check PHP Version
      run: php -v

    - name: Validate composer.json and composer.lock
      run: composer validate --strict

    - name: Install dependencies
      run: composer install --prefer-dist --no-progress

    - name: Run test suite
      run: make tests-unit

  jwt-parser-compatibility:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        parser-version: ['3.0', '4.0']
    name: PHP 8.4 with JWT Parser v${{ matrix.parser-version }}

    steps:
    - uses: actions/checkout@v2

    - name: Set Timezone
      uses: szenius/set-timezone@v1.0
      with:
        timezoneLinux: "Europe/Paris"

    - name: Install PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: 8.4
        extensions: json
        ini-values: date.timezone=Europe/Paris

    - name: Install dependencies
      run: composer install --prefer-dist --no-progress

    - name: Install JWT Parser v${{ matrix.parser-version }}
      run: composer require --dev web-token/jwt-library:~${{ matrix.parser-version }} -W

    - name: Run test suite
      run: make tests-unit

      # todo: matrix test with jwt parser library v3 and v4

  analysis:
    name: Analysis
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Set Timezone
      uses: szenius/set-timezone@v1.0
      with:
        timezoneLinux: "Europe/Paris"

    - name: Install PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: 8.4
        extensions: json
        ini-values: date.timezone=Europe/Paris

    - name: Validate composer.json and composer.lock
      run: composer validate --strict

    - name: Install dependencies
      run: composer install --prefer-dist --no-progress

    - name: Run type coverage
      run: make psalm-ci

    - name: Run Infection
      run: |
        git fetch --depth=1 origin $GITHUB_BASE_REF
        make infection-ci

    - name: Run PHPCS
      run: make phpcs
